
namespace editorjunk
{
    void TestUseSnappedLoc()
    {
        //while (true)
        //{
            //sleep(10);

            auto editor = cast<CGameCtnEditorFree>(GetApp().Editor);

            editor.Cursor.UseSnappedLoc = true;
        //}
    }
}
